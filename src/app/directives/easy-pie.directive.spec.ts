import { EasyPieDirective } from './easy-pie.directive';

describe('EasyPieDirective', () => {
  it('should create an instance', () => {
    const directive = new EasyPieDirective();
    expect(directive).toBeTruthy();
  });
});
