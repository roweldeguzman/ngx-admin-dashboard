import { StopPropagateDirective } from './stop-propagate.directive';

describe('StopPropagateDirective', () => {
  it('should create an instance', () => {
    const directive = new StopPropagateDirective();
    expect(directive).toBeTruthy();
  });
});
