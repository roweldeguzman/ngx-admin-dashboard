import { ToggleSubmenuDirective } from './toggle-submenu.directive';

describe('ToggleSubmenuDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSubmenuDirective();
    expect(directive).toBeTruthy();
  });
});
