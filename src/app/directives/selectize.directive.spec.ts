import { SelectizeDirective } from './selectize.directive';

describe('SelectizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectizeDirective();
    expect(directive).toBeTruthy();
  });
});
