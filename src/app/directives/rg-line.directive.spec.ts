import { RgLineDirective } from './rg-line.directive';

describe('RgLineDirective', () => {
  it('should create an instance', () => {
    const directive = new RgLineDirective();
    expect(directive).toBeTruthy();
  });
});
