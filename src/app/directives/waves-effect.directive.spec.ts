import { WavesEffectDirective } from './waves-effect.directive';

describe('WavesEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new WavesEffectDirective();
    expect(directive).toBeTruthy();
  });
});
