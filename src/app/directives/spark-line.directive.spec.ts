import { SparkLineDirective } from './spark-line.directive';

describe('SparkLineDirective', () => {
  it('should create an instance', () => {
    const directive = new SparkLineDirective();
    expect(directive).toBeTruthy();
  });
});
